export interface ICarousel{
  text: string;
  img: string;
}

export interface IArticles {
  id: number;
  name: string;
  title: string;
  date: string;
  read_time: number;
  category: string;
  tags: string[];
}